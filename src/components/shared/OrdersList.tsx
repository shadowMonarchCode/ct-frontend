
const OrdersList = (orders: Order[]) => {
  return (
    <div className="rounded-md border">
      {/* <DataTable columns={columns} data={orders} /> */}
    </div>
  );
};

export default OrdersList;

// OrderNum(filter) cust-name mobile item-tailored[check box]
