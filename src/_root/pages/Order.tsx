// import  from "@/components/shared/Demo";

const Order = () => {
  return (
    <div>
      {/* <Demo /> */}
      Helo
    </div>
  );
};

export default Order;

// orderId custId(phone + name) item-purchased(checkbox) measurement trialdate finishdate(deliverydate)
